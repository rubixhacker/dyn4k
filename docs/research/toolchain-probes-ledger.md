# Toolchain probe task ledger

Scope: [Probe the selected toolchain and unresolved consumer floors](https://github.com/rubixhacker/dyn4k/issues/11).
Base: `db4a13856aaf4bef089d1a93250971ff4e3d2f34`; branch `research/toolchain-probes`.

1. Completed: read canonical map, task, support checkpoints, charter and previous research; claim task as rubixhacker.
2. Completed: primary-source checks and bounded JVM/Android, web and Native probes; sources, tool digests and positive/negative evidence retained.
3. Completed: explicit 21-target/28-runtime matrix and configuration/harness gaps in toolchain-probes.md. Unsupported numeric minima remain identified gaps.
4. Completed local verification: parent reran Node/Wasmtime matrix (zero unexpected results), Java8 consumer, and final selected-JDK21 Linux debug/optimized artifacts (x64 direct and ARM64 QEMU); Native and JVM/Android artifact digest checks passed. Shell syntax checks passed. Publishing/tracker completion is recorded externally by immutable revision in the task resolution.

Final Native reproduction uses JDK21; earlier JDK25 exploration is explicitly retained separately. Android APK/R8 build evidence is not device execution. No Apple/Windows/browser/old-kernel execution is asserted. Human support reconciliation remains required.

No physics implementation, production provisioning, target exclusion, or numeric-floor ratification is authorized by this task.
